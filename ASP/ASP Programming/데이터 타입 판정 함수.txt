 종류 / 기능

IsArray(변수명) / 변수가 배열인지 여부를 반환

IsDate(식) / 식이 날짜로 변환될 수 있는지 여부를 반환

IsEmpty(식) / 식이 초기화되었는지 여부를 반환

IsNull(식) / 식이 Null인지 여부를 반환

IsNumeric(식) / 식이 숫자로 평가될 수있는지 여부를 반환

IsObject(식) / 식이 객체를 참조할 수 있는지 여부를 반환

